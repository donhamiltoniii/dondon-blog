---
import PostsList from "../../components/PostsList.astro";
import { RANDOM_IMAGE_URL, SITE_DESCRIPTION, SITE_TITLE } from "../../constants";
import Layout from "../../layouts/PageLayout.astro";
import { getPosts } from "../../utils/get-posts";

const posts = await getPosts({ numberOfPosts: "all" });
---

<Layout
  title={SITE_TITLE}
  description={SITE_DESCRIPTION}
  heroImage={RANDOM_IMAGE_URL}
>
  <main>
    <section>
      <PostsList posts={posts} />
    </section>
  </main>
</Layout>
