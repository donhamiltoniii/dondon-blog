---
import RecipesList from "../../../components/RecipesList.astro";
import { SITE_DESCRIPTION, SITE_TITLE } from "../../../constants";
import Layout from "../../../layouts/DefaultPageLayout.astro";
import { getFoodAssets } from "../../../utils/get-food-assets";

const recipes = await getFoodAssets();
const tags = [...new Set(recipes.flatMap((recipe) => recipe.data.tags).sort())];
---

<Layout title={SITE_TITLE} description={SITE_DESCRIPTION}>
  <h1>Recipe Tags</h1>
  <p>Tags! Click for recipes.</p>
  <ul>
    {
      tags.map((tag) => (
        <li>
          <a href={`/food/tags/${tag}`}>{tag}</a>
        </li>
      ))
    }
  </ul>
</Layout>
